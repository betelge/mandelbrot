
attribute vec3 position;

uniform mat4 modelViewMatrix;
uniform vec2 scale;
uniform vec2 offset;

varying vec2 c;

void main(void)
{
	vec4 pos = modelViewMatrix * vec4(position, 1.);
	c = 2.*(scale*pos.xy + offset);
	gl_Position = vec4(pos);
}